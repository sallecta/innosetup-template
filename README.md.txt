# innosetup-template
Helper scripts and tools for developing installers using innoSetup open source software.
Tested with InnoSetup 5.5.8 (u).

# Changes

Github web interface can not handle empty directories, so 02-installer.iss now takes care of this.